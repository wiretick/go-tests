package iteration

import "strings"

func Repeat(symbol string, nr int) string {
	return strings.Repeat(symbol, nr)

	//var result string
	//for i := 0; i < nr; i++ {
	//	result += symbol
	//}

	//return result
}
